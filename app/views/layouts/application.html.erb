<!DOCTYPE html>
<html>
	<head>
	  <title><%= full_title(yield(:title)) %></title> 
	  <!-- 可以是 media: "all" --><!-- 可以是 :media => "all" -->
	  <%= stylesheet_link_tag    "application", { :media => "all" } %>
	  <%= javascript_include_tag "application" %>
	  <%= csrf_meta_tags %>
	</head>
	<body>
	<%= yield %>
	</body>
</html>
